@@ -107,7 +107,11 @@
             .or_else(|| self.infer_from_node_id(req.node_id.as_deref()))
             .unwrap_or_else(|| ".".to_string());
             
-        let db_path = self.resolve_db_path_and_watch(Some(&inferred_root), None, None);
+        let path_buf = std::path::Path::new(&inferred_root);
+        let db_path = path_buf.join("knowledge.db").to_string_lossy().to_string();
+        // DO NOT START WATCHER DURING DEEP SCAN
+        // crate::watcher::ensure_watching(path_buf, &db_path);
+        
         let db_path_str = db_path.clone();
 
         // Automatically generate dump.lsif if it doesn't exist
